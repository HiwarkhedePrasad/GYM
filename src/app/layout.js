import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "New World Gym | Best Gym in Town",
  description: "Join New World Gym for state-of-the-art equipment, expert trainers, and a supportive community. Start your fitness journey today!",
  openGraph: {
    title: "New World Gym | Best Gym in Town",
    description: "Join New World Gym for state-of-the-art equipment, expert trainers, and a supportive community.",
    type: "website",
  },
};

export const viewport = {
  width: "device-width",
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
